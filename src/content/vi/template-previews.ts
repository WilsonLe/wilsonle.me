import { templatePreviews as enTemplatePreviews } from '@/content/en/template-previews'

// TODO(i18n): replace English fallback with reviewed Vietnamese demo copy.
export const templatePreviews: typeof enTemplatePreviews = enTemplatePreviews
